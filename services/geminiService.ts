import { Transaction, Category } from "../types";

/**
 * Đã gỡ bỏ logic AI để tăng tốc độ ứng dụng và tránh lỗi 404.
 */

export const getFinancialAdvice = async (): Promise<string> => {
  return "";
};

export const parseTransactionFromVoice = async (): Promise<any> => {
  return null;
};

export const parseTransactionFromImage = async (): Promise<any> => {
  return null;
};